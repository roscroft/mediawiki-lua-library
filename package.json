{
    "name": "mediawiki-lua-project",
    "version": "1.0.0",
    "description": "MediaWiki Lua Module Library",
    "scripts": {
        "lint": "npx markdownlint docs/*.md *.md --config .markdownlint.json",
        "lint:fix": "npx markdownlint docs/*.md *.md --fix --config .markdownlint.json",
        "format": "prettier --write docs/*.md *.md",
        "fix": "./scripts/auto-fix.sh",
        "postinstall": "chmod +x scripts/*.sh"
    },
    "devDependencies": {
        "markdownlint-cli": "^0.37.0"
    }
}